#include "main.h"
/**
 * mul-multiplies two integers.
 * @a:one of the integers to be multiplied
 * @b:integer to be multiplied with a
 * Return:Alway 0
 */

int mul(int a, int b)
{
	return (a * b);
}
