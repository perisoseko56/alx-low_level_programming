#include "main.h"
#include <stdio.h>

/**
 *_isdigit- checks for a digit
 *@c: characters to be checked.
 *Return:1 if c is a digits 0 otherwise
 */

int _isdigit(int c)
{
	if
		(c >= 48 && c <= 57)
		{
		return (1);
		}
	else
	{
	return (0);
	}
}
