#include <string.h>
#include "main.h"
#include <stdio.h>
/**
 * *_strstr-locates a substring
 * @haystack:string
 * @needle:substring
 * Return: Always o
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
